> svelte > tick
  ./hash.js
  ./tree.js

export default(
  scroll
  main
)=>
  for i from main.$$.ctx
    if i instanceof HTMLElement
      A = tree hash i # toc
      if A
        return [
          A
          scroll.$$.ctx[0].$$.ctx[0]
        ]
      break
  return

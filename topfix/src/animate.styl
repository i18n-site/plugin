@keyframes inD
  from
    transform translate3d(0, -100%, 0)
    visibility visible

  to
    transform translate3d(0, 0, 0)

@keyframes oUp
  from
    transform translate3d(0, 0, 0)

  to
    transform translate3d(0, -100%, 0)
    visibility hidden
